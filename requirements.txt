paho-mqtt==1.6.1
python-dotenv==1.0.0

# QR decoding (no GUI — headless-safe for Pi OS)
opencv-python-headless>=4.8.0

# Hardware deps (GPIO/servo, Pi camera) are Pi-only and live in requirements-pi.txt
# so this file stays installable on a laptop. On the Pi:
#   pip install -r requirements.txt -r requirements-pi.txt

# Pi Camera Module V2 driver
# Install via apt on the Pi (preferred — includes libcamera native bindings):
#   sudo apt install -y python3-picamera2
# Or via pip (may need extra system libs):
#   pip install picamera2
# picamera2
