# Extra dependencies for the laptop test harness (tests/laptop/).
# These are NOT needed to run the RPi device itself — see ../../requirements.txt
# for the device runtime deps (paho-mqtt is shared and listed there).

# QR-code generation for mock_backend.py --gui (stands in for the app's QR role).
qrcode>=7.4
Pillow>=10.0

# paho-mqtt is already pinned in ../../requirements.txt and is required by every
# script in this folder.
